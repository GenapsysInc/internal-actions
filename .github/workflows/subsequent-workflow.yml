name: subsequent-workflow

on:
  workflow_run:
    workflows: [example-calling-workflow]
    types:
      - completed

jobs:
  job-8:
    name: Job 8
    runs-on: ubuntu-latest
    steps:
      - name: Say we got called
        run: echo "Job 8 was run"
