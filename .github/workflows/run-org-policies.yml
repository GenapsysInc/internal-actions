name: Org Policies

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  schedule:
    - cron: '35 17 * * 0'

jobs:
  run-jobs:
    name: Org Policy Check
    uses: GenapsysInc/internal-actions/.github/workflows/org-policies.yml@set-output-updates-test
    secrets:
      token: ${{ secrets.GH_REST_API_TOKEN }}
